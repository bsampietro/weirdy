$this = $("#wexception_<%= params[:id] %>");
$occurrences = $this.next(".occurrences");
$this.fadeOut(400, function(){ $this.remove(); });
if ($occurrences.length > 0)
  $occurrences.fadeOut(400, function(){ $occurrences.remove(); });
